{
  "name": "${project.artifactId}",
  "version": "${version.unrevise}",
  "description": "${project.description}",
  "dependencies": {
    "@angular/animations": "${version.unrevise}",
    "@angular/common": "${version.unrevise}",
    "@angular/compiler": "${version.unrevise}",
    "@angular/core": "${version.unrevise}",
    "@angular/elements": "${version.unrevise}",
    "@angular/forms": "${version.unrevise}",
    "@angular/platform-browser": "${version.unrevise}",
    "@angular/platform-browser-dynamic": "${version.unrevise}",
    "@angular/platform-server": "${version.unrevise}",
    "@angular/router": "${version.unrevise}",
    "@angular/service-worker": "${version.unrevise}",
    "@angular/upgrade": "${version.unrevise}"
    }
}
